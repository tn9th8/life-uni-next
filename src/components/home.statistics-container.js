import style from "../app/styles.module.scss";

function HomeStatisticsContainer({ children }) {
  return <div className={style.homeStatisticsContainer}>{children}</div>;
}

export default HomeStatisticsContainer;
